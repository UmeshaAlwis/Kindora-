name: kindora
description: "A donation and social impact platform."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8

  # Icons and Fonts
  lucide_icons: ^0.257.0
  google_fonts: ^6.2.1

  # NEW: Added to fix "Couldn't resolve package" errors
  cached_network_image: ^3.4.1
  percent_indicator: ^4.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true